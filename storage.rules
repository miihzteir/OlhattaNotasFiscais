rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /notas-anexos/{allPaths=**} {
      allow read, write: if request.auth != null
        && request.auth.token.email in [
          'francimeryapfachini@gmail.com',
          'draoftalmologistavet@gmail.com'
        ];
    }
  }
}
